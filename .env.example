# Path to the saved model artefact used by the Streamlit app.
CHURN_MODEL_PATH=models/churn_pipeline.joblib
# Port the app listens on when started via `streamlit run`.
STREAMLIT_SERVER_PORT=8531
# Password for the app's model-management page. Leave unset to keep the page locked.
CHURN_ADMIN_PASSWORD=
